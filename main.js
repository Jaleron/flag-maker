const flag = document.getElementById('flag');
const field = document.getElementById('field');
const fieldSVG = document.getElementById('fieldSVG');

let flagWidth = 360;
let flagHeight = 240;

flag.style.width = `${flagWidth}px`;
flag.style.height = `${flagHeight}px`;

field.style.width = `${flagWidth}px`;
field.style.height = `${flagHeight}px`;

fieldSVG.style.width = `${flagWidth}px`;
fieldSVG.style.height = `${flagHeight}px`;